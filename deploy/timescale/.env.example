# Copy to .env (gitignored) and set a strong password.
# The deploy script generates one automatically if .env is missing.
POSTGRES_DB=factor
POSTGRES_USER=factor
POSTGRES_PASSWORD=change-me
PGDATA_DIR=/volume1/factor-data/pgdata
DB_HOST_PORT=5433
